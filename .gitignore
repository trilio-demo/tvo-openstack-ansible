# Credentials — never commit these
secure.yaml
vars/vault.yml
vars/main.yml
vars/openrc/*.sh
!vars/openrc/*.sh.sample
clouds.yaml
cloud-init-password.yaml
*.password
.env

# Project-specific / not for public repo
collateral/
framework/
inventory/
OSP-Demos.code-workspace

# Per-clone, customer-identifying local context (auth IDs, investigations,
# Session State) — auto-loaded by Claude Code at cold start, never committed
CLAUDE.local.md

# Ad-hoc notes, writeups, transient session artifacts — never commit
notes/

# Python venv
.venv/

# Ansible retry files
*.retry

# OS
.DS_Store

# Editor backup files (e.g. vars/vault.yml~)
*~
*.swp
*.swo
